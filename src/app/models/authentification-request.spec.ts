import { AuthentificationRequest } from './authentification-request';

describe('AuthentificationRequest', () => {
  it('should create an instance', () => {
    expect(new AuthentificationRequest()).toBeTruthy();
  });
});
